# Clerk publishable key (safe to expose in the browser).
# Get it from https://dashboard.clerk.com → API Keys → Publishable key.
# In Vercel: add this under Project Settings → Environment Variables for all environments.
VITE_CLERK_PUBLISHABLE_KEY=
